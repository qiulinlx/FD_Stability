description: "Regularized with more rounds"

n_rounds: 250
early_stopping_rounds: 15

params:
  objective: "reg:squarederror"
  max_depth: 6
  min_child_weight: 5
  gamma: 0.1
  reg_lambda: 2.0
  reg_alpha: 0.5
  tree_method: "approx"
  learning_rate: 0.01